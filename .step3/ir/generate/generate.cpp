#include "ir/generate/generate.h"

namespace sysy::ir {
IRList generate(sysy::ast::node::BaseAST* root) {
  //Context ctx;
  IRList ir;
  //root->generate_ir(ctx, ir);
  return ir;
}
}  // namespace sysy::ir